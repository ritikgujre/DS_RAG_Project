anthropic>=1,<2
sentence-transformers>=6.0.0
rank-bm25>=0.2.2
pymupdf>=1.28.0
numpy>=2.0
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9

# Local GPU backend (--backend local). Needs a CUDA build of torch, which pip
# will NOT pick by default -- the plain PyPI wheel is CPU-only. Install it from
# the matching CUDA index first, e.g. for CUDA 13:
#   pip install --index-url https://download.pytorch.org/whl/cu130 torch==2.13.0+cu130
accelerate>=1.14
bitsandbytes>=0.50
